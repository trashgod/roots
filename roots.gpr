project Roots is

   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Create_Missing_Dirs use "True";
   for Main use ("croot.adb");

   package Compiler is
      for Switches ("Ada") use ("-gnatwa");
   end Compiler;

end Roots;
